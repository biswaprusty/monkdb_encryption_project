// placeholder routes file (optional - not used by migrations)
// keep for future Express demo; left intentionally minimal
module.exports = function(router) {
  router.get('/ping', (req, res) => res.json({ ok: true }));
};
