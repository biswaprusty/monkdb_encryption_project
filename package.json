{
  "name": "monkdb-encryption-project",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "check": "node scripts/check_run.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "pg": "^8.10.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
